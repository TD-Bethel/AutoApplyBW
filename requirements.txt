Flask>=3.0
python-dotenv>=1.0
requests>=2.31
pypdf>=4.0
fpdf2>=2.8
waitress>=3.0
# Pure-Python PostgreSQL driver (no native wheels). Only used when DATABASE_URL
# is set; SQLite installs never import it. Enables running many app instances
# against one shared Postgres for high-scale, high-availability deployments.
pg8000>=1.30
# Error tracking (pure Python). Only activated when SENTRY_DSN is set; the app
# runs fine without the package or the DSN (lazy import in app/observability.py).
sentry-sdk>=2.0
